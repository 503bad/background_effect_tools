#pragma once
/* Geometric pattern backgrounds: tiled / scattered abstract motifs selectable by
 * mode — triangle tiles, hexagon grid, translucent diamonds + slashes, and a
 * scattered "Memphis" layer. Time-driven (scroll / colour-cycle / pulse) so it
 * animates with no audio; audio reactivity (level + beat) adds extra motion. */
