#pragma once
/* Voice Explode: particles erupt up from the bottom only while the chosen
 * audio source pushes past a threshold; jet angle/spread and an optional
 * frequency band (e.g. a kick) are configurable. */
